root = "."
tmp_dir = "tmp"

[build]
  cmd = "go tool templ generate && go build -o ./tmp/main ./cmd/server"
  bin = "tmp/main"
  include_ext = ["go", "templ"]
  exclude_dir = ["tmp", "vendor"]
  exclude_regex = ["_templ.go"]

[log]
  time = true
